# Check Project Status

1. Check if all dependencies are installed (npm ls)
2. Check if TypeScript compiles (npx tsc --noEmit)
3. Check if dev server starts
4. List implemented endpoints from registry
5. Check if data/ and output/ exist
6. Summarize what works and what needs attention
