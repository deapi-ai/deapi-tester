# Test Endpoint

Execute a test request to specified deAPI endpoint.
1. Load configuration from data/config.json
2. Use curl to send request with appropriate headers
3. Show full request and response
4. If async — poll result endpoint until completion
