# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# claude local
/.claude/settings.local.json

# misc
.DS_Store
*.pem
nul

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# output directory (generated files)
/output

# local data files (contain sensitive keys and user history)
# Ignore all local data json (config holds the API token, history holds requests),
# but keep the committed example. config.json is created on first save, never shipped.
/data/*.json
!/data/config.example.json
/data/config.json
/data/history.json
/data/uploads

# local vscode settings
.vscode/settings.json